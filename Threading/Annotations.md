@Override
→ Indicates method overriding.

Annotations
→ Metadata attached to code.

Reflection
→ Inspect and manipulate classes/methods at runtime.

instanceof
→ Checks object type.

Compile-Time Annotation
→ Used by compiler.

Runtime Annotation
→ Available during execution via Reflection.
